/*
In this code we will see how a program with one dimension array looks like. This is a normal array you have worked with before.
In this program we will work on an CRUD that accept student grades, save into the array, print when its asked, update the student grade and delete students from the array.
*/

#include <stdio.h>

int main(void)
{
    int numberOfStudents =0;

    scanf("%d", &numberOfStudents);
    return 0;
}